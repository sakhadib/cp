#include<stdio.h>
int main()
{
    int a,b,c,d,t,i;

    scanf("%d",&t);

    for (i=0;i<t;i++)
    {
        scanf("%d %d %d",&a,&b,&c);

        if(a>=b && a>=c)
            big = a
    }

    return 0;
}
