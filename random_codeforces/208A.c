#include<stdio.h>
#include<string.h>
int main()
{
    char a[200];
    int i,len;

    scanf("%s",a);
    len = strlen(a);

    printf("%d  \0 k",len);

    for(i=0;i<len;i++)
    {

    }


    return 0;
}
